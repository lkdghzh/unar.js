/**
 * Get the difference between the two virtual dom
 * param a :old virtual dom
 * param b :new virtual dom
 * return patch:the difference
 */

 /**
  * use dfs algorithm
  * mark for every node width inx 0 1 2 ...
  */
const dfs=(a,b,inx,patches)=>{
    
}
export const diff = (a, b) => {
    let patches = {}
    return patches
}
