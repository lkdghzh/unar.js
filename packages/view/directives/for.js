import Base from './base'

export default class For extends Base{
    constructor(opts) {
        super(opts)
        this.exp = opts.expOrFn
    }
    bind() {

        super.bind(cb)
    }
}